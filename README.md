# Rhett Boyakin — portfolio

Creative Director. Copywriter. Maker of Awesome Thingys®.

This replaces the locked Adobe Portfolio at rhettboyakinportfolio.com. Agency work is archived here. Revenue Bloom sits first.

## Local
Open `index.html`, or:

```
python3 -m http.server 8080
```

## GitHub Pages
Published from `main` at:

https://rhettboyakin-code.github.io/portfolio/

## Custom domain later
When Adobe releases `rhettboyakinportfolio.com`, point DNS here (GitHub Pages custom domain) and add a `CNAME` file with that host. Until then, the Adobe site can stay up as a backup.

## Edit
Work pages live in `work/`. Images in `img/<slug>/`. Copy is in `build.py` if you want to regenerate, or just edit the HTML.
